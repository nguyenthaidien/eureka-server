name: Example Self-Hosted Workflow

on:
  push:
    branches:
      - main

jobs:
  example-job:
    runs-on: self-hosted
    steps:
      - name: Install Dependencies
        run: |
          echo "Installing dependencies..."
          mkdir /opt/actions-runner/workings/diennt